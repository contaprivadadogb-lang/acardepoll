{
  "name": "acardepoll",
  "version": "1.0.0",
  "scripts": {
    "android": "npx cap sync && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
